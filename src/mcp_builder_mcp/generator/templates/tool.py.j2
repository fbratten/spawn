"""{{ tool.name }} implementation.

{{ tool.description }}

Auto-generated by Reproducible Pattern MCP Builder.
"""

from pathlib import Path
from typing import Any

{% if tool.imports %}
{% for imp in tool.imports %}
{{ imp }}
{% endfor %}
{% endif %}


def {{ tool.function }}_impl(
{%- for param in tool.params %}
    {{ param.name }}: {{ param.type }}{% if param.default is not none %} = {{ param.default | tojson }}{% endif %},
{%- endfor %}
) -> dict[str, Any]:
    """{{ tool.description }}

{% for param in tool.params %}
    Args:
        {{ param.name }}: {{ param.description }}
{% endfor %}

    Returns:
        {{ tool.returns }}
    """
    # TODO: Implement {{ tool.function }}
{% for step in tool.steps %}
    # Step {{ loop.index }}: {{ step.description }}
{% endfor %}

    return {
        "status": "not_implemented",
        "message": "{{ tool.function }} needs implementation",
    }
