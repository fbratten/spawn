# {{ pattern.name }} - MCP Server Documentation

> **Auto-generated by Reproducible Pattern MCP Builder**
> **Pattern ID**: {{ pattern.id }}
> **Version**: {{ version }}

---

## MCP Server Information

| Property | Value |
|----------|-------|
| **Server Name** | `{{ server_name }}` |
| **Package Name** | `{{ package_name }}` |
| **Protocol** | Model Context Protocol (MCP) |
| **Version** | {{ version }} |
| **Category** | {{ pattern.category }} |
| **Tags** | {{ pattern.tags | join(', ') if pattern.tags else 'none' }} |

### Tools Provided

| Tool | Description |
|------|-------------|
{% for tool in tools %}
| `{{ tool.function }}` | {{ tool.description[:80] }}{% if tool.description|length > 80 %}...{% endif %} |
{% endfor %}

---

## Features

{% if pattern.workflow_steps %}
### Core Workflow
{% for step in pattern.workflow_steps %}
{{ loop.index }}. **{{ step.action }}**: {{ step.description }}
{% endfor %}
{% endif %}

### Trigger Phrases

The following user requests will trigger this MCP:

{% for trigger in pattern.triggers %}
- "{{ trigger.phrase }}"
{% endfor %}

### Input/Output

{% if pattern.inputs %}
**Inputs:**
{% for inp in pattern.inputs %}
- `{{ inp.name }}` ({{ inp.type }}{% if not inp.required %}, optional{% endif %}): {{ inp.description if inp.description else 'No description' }}
{% endfor %}
{% else %}
**Inputs:** None required
{% endif %}

{% if pattern.outputs %}
**Outputs:**
{% for out in pattern.outputs %}
- `{{ out.name }}` ({{ out.type }}): {{ out.format if out.format else 'standard format' }}
{% endfor %}
{% else %}
**Outputs:** Returns result dictionary
{% endif %}

---

## Capabilities

### What This MCP Can Do

{% if pattern.workflow_steps %}
{% for step in pattern.workflow_steps %}
- ✅ {{ step.description }}
{% endfor %}
{% else %}
- ✅ {{ pattern.description }}
{% endif %}

### Integration Points

- **Claude Code**: Direct tool invocation
- **Claude Desktop**: Via MCP server configuration
- **Other AI Assistants**: Any MCP-compatible client
- **Automation**: Can be scripted via MCP protocol

### Dependencies

{% if dependencies %}
{% for dep in dependencies %}
- `{{ dep }}`
{% endfor %}
{% else %}
- Standard Python library only
{% endif %}

---

## Use Cases

### Primary Use Case

**{{ pattern.name }}**

{{ pattern.description }}

**When to use:**
{% for trigger in pattern.triggers %}
- When user says: "{{ trigger.phrase }}"
{% endfor %}

### Common Scenarios

1. **Routine Automation**
   - Automating repetitive tasks matching the pattern triggers
   - Reducing manual intervention for known workflows

2. **Consistency**
   - Ensuring the same process is followed each time
   - Standardizing workflow execution

3. **Efficiency**
   - Faster execution than manual steps
   - Reduced cognitive load for common operations

---

## Fringe Use Cases

### Creative Applications

1. **Batch Processing**
   - Apply this workflow to multiple targets in sequence
   - Combine with other MCPs for pipeline processing

2. **Template/Starting Point**
   - Use generated output as template for similar tasks
   - Modify workflow for related but different needs

3. **Learning/Documentation**
   - Reference for understanding the workflow steps
   - Training material for similar manual processes

### Edge Cases

1. **Partial Execution**
   - Running only specific steps of the workflow
   - Stopping after validation/check phases

2. **Dry Run Mode**
   - Preview what would happen without making changes
   - Useful for verification before committing

3. **Integration with Other Tools**
   - Chaining with other MCPs (e.g., run this after `smart-inventory`)
   - Triggering from CI/CD pipelines or cron jobs

### Limitations

- This MCP is generated from observed patterns; edge cases may need handling
- Complex scenarios may require manual intervention
- Always verify output in critical workflows

---

## Configuration

### MCP Server Setup

Add to your MCP configuration (`.mcp.json` or Claude settings):

```json
{
  "{{ package_name }}": {
    "command": "python",
    "args": ["-m", "{{ package_name }}.server"],
    "cwd": "/path/to/{{ package_name }}"
  }
}
```

### Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `LOG_LEVEL` | Logging verbosity | `INFO` |
| `OUTPUT_DIR` | Default output directory | `.` |

---

## Technical Details

### Pattern Origin

- **Extracted From**: {{ pattern.extracted_from if pattern.extracted_from else 'User-defined' }}
- **Extraction Date**: {{ pattern.extraction_date if pattern.extraction_date else 'N/A' }}
- **Confidence**: {{ "%.0f"|format(pattern.confidence * 100) if pattern.confidence else 'N/A' }}%

### Architecture

```
┌─────────────────────────────────────────┐
│           {{ server_name }}             │
├─────────────────────────────────────────┤
│                                         │
│  {% for tool in tools %}┌─────────────────┐  │
│  │ {{ tool.function[:15] }}{% for _ in range(15 - tool.function|length) %} {% endfor %}│  │
│  └─────────────────┘  │
│  {% endfor %}                                         │
│                                         │
└─────────────────────────────────────────┘
```

### File Structure

```
{{ package_name }}/
├── src/
│   └── {{ package_name }}/
│       ├── __init__.py
│       ├── server.py          # MCP server entry point
│       └── tools/
│           └── *.py           # Tool implementations
├── tests/
│   └── test_{{ package_name }}.py
├── pyproject.toml
├── README.md
├── SKILL.md
└── MCP_INFO.md               # This file
```

---

## Changelog

### v{{ version }} (Initial)

- Generated from pattern: {{ pattern.id }}
- Implements: {{ pattern.name }}

---

*Generated by Reproducible Pattern MCP Builder*
*Pattern source: {{ pattern.extracted_from if pattern.extracted_from else 'AI-defined' }}*
