# {{ pattern.name }}

{{ pattern.description }}

> Auto-generated by [Reproducible Pattern MCP Builder](https://github.com/example/pattern-mcp)

## Installation

```bash
# Create virtual environment
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate   # Windows

# Install package
uv pip install -e ".[dev]"
```

## Usage

### As MCP Server

Add to your Claude Code configuration:

```json
{
  "mcpServers": {
    "{{ package_name }}": {
      "command": "{{ package_name }}",
      "args": []
    }
  }
}
```

### Available Tools

{% for tool in tools %}
#### `{{ tool.function }}`

{{ tool.description }}

**Parameters:**
{% for param in tool.params %}
- `{{ param.name }}` ({{ param.type }}{% if not param.required %}, optional{% endif %}): {{ param.description }}
{% endfor %}

{% endfor %}

## Development

```bash
# Run tests
pytest tests/ -v

# Lint
ruff check .

# Type check
pyright
```

## License

MIT

---

*Generated from pattern: {{ pattern.id }}*
